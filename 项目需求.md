# 项目需求 — dsh-vision-triage（视觉分诊）

> 本文件是 DSH 视觉分级处理插件的**需求与架构蓝图**。当前 `dsh-vis-it` 目录是一次探索性原型，
> 存在数据混乱、目录职责不清等问题，后续将**重开项目目录、依据本文档从头实现**。

## 1. 项目定位

**一句话**：为 DSH（DeepSeek Harness）提供一套**分级视觉处理流水线**——先用轻量本地分类器判断图像的
"内容构成类型"，再把图片分流到最合适的处理工具（YOLO / OmniParser / OCR / QwenVL），
从而在**保证可用性的前提下，把简单任务留在本地、把昂贵的云端 VLM 只留给真正需要语义理解的追问环节**。

**名字含义**：`triage`（分诊）——像急诊分诊一样，先快速判断"这是什么类型的问题"，再决定送哪个科室处理。

## 2. 背景与问题

当前 DSH 的图像处理路径：

```
图片 ──► vision-mcp（describe_image）──► QwenVL（云端多模态大模型）
```

所有图片一律送云端大模型，存在三个问题：

1. **成本高**：简单图片（纯文本截图、表单）也按完整图片走云端 token 计费。
2. **延迟高**：云端往返 + 大模型推理，简单任务本可以毫秒级本地完成。
3. **精度错配**：表单/文本用专用 OCR 更准，UI 用 OmniParser 更结构化，检测用 YOLO 更擅长定位；
   通用 VLM 在这些任务上并非最优。

**核心诉求**：简单、确定的任务留在本地专用模型；QwenVL 只做语义追问，不做重复识别。

## 3. 核心架构：分级流水线

### 3.1 图像构成的本质分类（设计哲学）

依据图像**内容如何表达含义**分为两大类（这是整个分类体系的根基）：

| 类别 | 含义 | 语义载体 | 典型例子 |
|---|---|---|---|
| **内容承载型** content | 靠图像像素内容本身表达 | 物体、场景、光影、透视 | 照片、CG 渲染、二次元插画、画作、海报主体 |
| **结构承载型** structure | 靠文字与布局规则表达 | 字体、控件、表格线、排版 | 纯文本、表单、软件 UI、网页、文档 |

> 依据：自然图像（含 拍摄/CG/插画）本质是"透视 + 对象化分层"；人工界面本质是"图元组合 + 显式空间关系"
> （详见 `D:/pytorchPro/study_network/2_CNN/README.md` 的思考）。两类构成过程差异巨大，应分开处理。

### 3.2 两级分类器（全部轻量 yolo-classify，秒级）

```
                        ┌─────────────────────────────┐
  图片 ────────────────► │  L1 大分类 (yolo-classify)   │
                        │  content vs structure        │
                        └──────────────┬──────────────┘
                                       │
              ┌────────────────────────┴────────────────────────┐
              ▼                                                 ▼
     content (内容承载)                                 structure (结构承载)
     → YOLO / YOLO-World 本地检测                           │
     → 细节/语义 → QwenVL 追问                    ┌────────────────────────┐
                                                 │  L2 细分 (yolo-classify) │
                                                 │  ui / text / form       │
                                                 └──────┬─────────┬───────┘
                                                        ▼         ▼
                                                ui → OmniParser   text/form → OCR
                                                (元素/布局)         (文本/表格)
```

- **L1**：`content`（照片/CG/插画） vs `structure`（文本/表单/UI）
- **L2**（仅 structure 内部）：`ui` / `text` / `form`
- **嵌套不归分类器管**：UI 内的照片/表单区域由 OmniParser 逐元素识别
  （已验证：OmniParser 会把内嵌图像检测为一个元素并用 Florence-2 生成语义描述）
- **OmniParser 不参与分类决策**：它加载 1.3GB 模型需 15-25s，分类只用轻量模型

### 3.3 提供的工具（5 个，LLM 自主组装）

| # | 工具名 | 类型 | 职责 | 后端 |
|---|---|---|---|---|
| ① | `classify_image` | 本地 | L1 大分类：content vs structure | yolo-classify |
| ② | `classify_structure` | 本地 | L2 细分：ui / text / form | yolo-classify |
| ③ | `detect_natural_image` | 本地 | 内容图像目标检测（COCO 80 类） | YOLO11 / YOLO-World |
| ④ | `parse_ui_screenshot` | 本地 | UI 结构化解析（元素/布局） | OmniParser v2 |
| ⑤ | `ocr_image` | 本地 | OCR 文本提取 + 可选表格 | RapidOCR |
| ⑥ | `describe_image` | 云端 | QwenVL 语义追问（按需） | Qwen-VL |

工具说明（description）中写入**推荐使用流程**与**阈值/回退策略**，由 DSH 的 LLM 自主决定如何组装，
**不做固定程序化编排**。

### 3.4 关键决策记录

1. **分类器**：轻量 yolo-classify 训练，两级（L1 二分类 + L2 三分类）。
2. **本地推理**：仅 PyTorch（自用，不做 ONNX/OpenVINO 适配）。
3. **数据**：全部用**网络真实数据**（软件名搜索截图 / 真实照片），不用合成数据；
   每类 1000 张（train 800 + val 200），混入少量噪声可接受。
4. **不造轮子**：OCR 用 RapidOCR、UI 解析用 OmniParser、检测用 YOLO，全部现成。
5. **失败回退**：分类置信度 < 0.6 时降级（直接走 QwenVL 或交叉验证），保证不劣于现状。
6. **OmniParser 成本高**：只做 UI 结构化输出，不参与分类路由。

## 4. 参考项目与技术（重要）

### 4.1 基础插件（融合改造的对象）

- **dsh-vision-mcp**：https://github.com/CeasarSmj/dsh-vision-mcp
  - DSH 官方生态的视觉 MCP 插件，提供 `describe_image`（Qwen-VL）。
  - 本插件以其为**融合基础**：保留云端 `describe_image`，新增本地工具。
  - 安装方式：junction 挂载到 profile 的 node_modules + `cordis.patch.yml` 配置。

### 4.2 现成模型与库（避免造轮子）

| 用途 | 方案 | 参考链接 |
|---|---|---|
| 自然图像检测 | Ultralytics YOLO11（COCO 预训练） | https://github.com/ultralytics/yolo11 |
| 开放词汇检测 | YOLO-World（任意文本提示零样本） | https://docs.ultralytics.com/models/yolo-world |
| UI/界面解析 | Microsoft OmniParser v2（YOLO + Florence-2） | https://github.com/microsoft/OmniParser |
| 轻量零样本分类 | Apple MobileCLIP | https://huggingface.co/apple/MobileCLIP |
| OCR 文本提取 | RapidOCR（ONNX，PaddleOCR 移植） | https://github.com/RapidAI/RapidOCR |
| 表格结构识别 | RapidAI TableStructureRec | https://github.com/RapidAI/TableStructureRec |
| 中文 OCR（备选） | PaddleOCR（含表格结构化） | https://github.com/PaddlePaddle/PaddleOCR |

### 4.3 关键经验（原型阶段踩坑，重做时直接规避）

1. **transformers 版本**：Florence-2（OmniParser 依赖）与 transformers 5.x 不兼容
   （`EncoderDecoderCache`、`_supports_sdpa`、tied-weights dict 等 API 变更）。
   **必须用 transformers 4.x**（4.57.2 已验证），并可能需修补 HF 缓存的 remote code。
2. **paddlepaddle 难装**：Python 3.13 下 paddle 通常装不上；OmniParser 的 paddle 硬依赖需裁剪，
   OCR 分支改走 EasyOCR/RapidOCR。
3. **OmniParser 空 OCR bug**：纯内容图（无文字）会导致管道崩溃（`zip(None)` 与裸 list 元素），
   需修补：空 OCR 用 `[]` 而非 `None`、无 OCR 分支保持 dict 结构。
4. **本地环境**：`E:\conda\envs\sdenv\python.exe`（Python 3.13 + torch 2.13 + CUDA），
   可用环境变量 `DSH_VISIT_PYTHON` 覆盖。
5. **插件依赖解析**：junction 挂载的插件内部 import `@deepseek-ai/dsh-tools` 时，
   需在插件目录内加 node_modules junction 指向 profiles/node_modules。

## 5. 验收标准

1. DSH 重启后模型工具目录出现 6 个工具（①~⑥）。
2. 四类测试图（照片/文本/表单/UI）分类正确，路由到对应工具。
3. `describe_image` 仅按需调用，简单图片不触发云端请求。
4. 全部本地工具在 DSH 会话内可直接调用并返回结构化 JSON。

## 6. 里程碑

- [ ] M1：重开项目目录，按本文档搭骨架（两级分类器 + 5 工具注册）
- [ ] M2：真实数据下载（natural/UI/text/form 各 1000）并训练 L1/L2 分类器
- [ ] M3：接入 DSH，验证 6 工具可用与 LLM 自主组装
- [ ] M4：真实负载下成本/延迟/精度对比评测
